runtime: go
env: flex

runtime_config:
  operating_system: "ubuntu22"
  runtime_version: 1.19

service: api

instance_class: F2

env_variables:
  DB_USERNAME:
  DB_PASSWORD:
  DB_NAME:
  DB_PORT:
  DB_HOSTNAME:
  INSTANCE_CONNECTION_NAME:
  HTTP_PORT:

beta_settings:
  cloud_sql_instances: "the-new-orleans-connection:us-central1:qr-code-instance"

inbound_services:
  - warmup
